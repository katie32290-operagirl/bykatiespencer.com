/**
 * Routes that have been rebuilt in the warm redesign and carry their own
 * nav/footer. The old Opening Night global chrome is suppressed on these.
 */
export const REDESIGNED = new Set<string>([
  "/",
  "/about",
  "/portfolio",
  "/narratives",
  "/writing",
  "/contact",
]);
